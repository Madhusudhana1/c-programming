#include <stdio.h>

int main() {
    int n, i, even_num = 2;
    
    printf("Enter the value of n: ");
    scanf("%d", &n);
    
    printf("The first %d even numbers are: ", n);
    for (i = 1; i <= n; i++) {
        printf("%d ", even_num);
        even_num += 2;
    }
    
    return 0;
}
